name: nomade_domain
description: Domain models and business logic for Nomade
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.11.0
  equatable: ^2.0.5
  json_annotation: ^4.8.1

dev_dependencies:
  test: ^1.24.9
  json_serializable: ^6.7.1
  build_runner: ^2.4.7
  lints: ^5.0.0
